#include "line.h"
#include "rectangle.h"

int parseInput(FILE *in, Lines lines, Rectangles rects)
{
  int id;
  char type[10], name[12];
  Point p1, p2;
  // while(fscanf("%d %s %s (%lf, ) ()") == 4)
}